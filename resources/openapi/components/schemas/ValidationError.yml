type: object
required:
  - message
  - errors
properties:
  message:
    type: string
    example: The email field is required.
  errors:
    type: object
    additionalProperties:
      type: array
      items:
        type: string
    example:
      email:
        - "The email field is required."
